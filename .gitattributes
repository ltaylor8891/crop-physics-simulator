# Normalise line endings: LF in the repository and working tree on all platforms
# (matches Prettier's endOfLine: "lf" and keeps diffs free of CRLF churn).
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
